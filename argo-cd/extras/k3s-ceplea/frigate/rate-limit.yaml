---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: rate-limit
spec:
  # Generous: the UI loads thumbnail grids and video segments in bursts.
  rateLimit:
    average: 50
    burst: 200
    period: 1s
