secrets:
  # Disabled: S3 backups to k3s-buc are disabled.
  # Uncomment to re-enable (needs the matching 1Password items updated to the new target).
  # - name: longhorn-backups-s3-creds
  #   external_secret_id: Longhorn Backups S3 Creds
  #   data:
  #     - secret_key: AWS_ACCESS_KEY_ID
  #     - secret_key: AWS_SECRET_ACCESS_KEY
  #     - secret_key: AWS_ENDPOINTS

  # - name: longhorn-backups-s3-creds-garage-archive
  #   external_secret_id: Longhorn Backups S3 Creds - garage-archive
  #   data:
  #     - secret_key: AWS_ACCESS_KEY_ID
  #     - secret_key: AWS_SECRET_ACCESS_KEY
  #     - secret_key: AWS_ENDPOINTS
