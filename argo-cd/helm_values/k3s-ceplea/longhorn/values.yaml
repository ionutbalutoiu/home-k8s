preUpgradeChecker:
  jobEnabled: false

defaultSettings:
  fastReplicaRebuildEnabled: true
  snapshotDataIntegrity: enabled
  snapshotDataIntegrityImmediateCheckAfterSnapshotCreation: true
  snapshotDataIntegrityCronjob: "0 3 * * *"

persistence:
  defaultClass: true
  defaultFsType: ext4
  defaultClassReplicaCount: 2
  defaultDataLocality: best-effort
  reclaimPolicy: Delete

httproute:
  enabled: true
  parentRefs:
    - name: traefik-gateway
      namespace: traefik-internal
  hostnames:
    - longhorn.internal.balutoiu.com

metrics:
  serviceMonitor:
    enabled: true

# Longhorn chart 1.12.1+ renders 6 NetworkPolicies from
# networkPolicies.restrictInternalTraffic (default true), which blocks
# cross-namespace metrics scraping of longhorn-manager (upstream longhorn#13740).
# This cluster does not use NetworkPolicies; keep all of them disabled.
networkPolicies:
  enabled: false
  restrictInternalTraffic: false

# Disabled: S3 backups to the k3s-buc garage-archive target.
# Uncomment to re-enable (needs the matching 1Password creds for the new target).
# defaultBackupStore:
#   backupTarget: s3://longhorn-backups@garage/
#   backupTargetCredentialSecret: longhorn-backups-s3-creds
